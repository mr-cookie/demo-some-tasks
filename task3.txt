
1.

SELECT * 
FROM a,b 
WHERE a.id = b.a_id;

2. 
SELECT * 
FROM a
JOIN b ON a.id = b.a_id

--  Результат запросов будет одинаков, только синтаксис в 1-м примере является устаревшим и может не поддерживаться производителями некоторых СУБД.
